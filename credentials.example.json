{
  "_comment": "Copy to credentials.json (gitignored) and replace with the credentials you are authorized to test. Nothing here is a real credential. You do NOT need this file for vendor factory defaults - those are bundled and selected by device fingerprint with --default-creds (see 'nwaa profiles'). Use this file for site-specific credentials the client gave you.",
  "credentials": [
    {
      "username": "REPLACE_ME_USERNAME",
      "password": "REPLACE_ME_PASSWORD",
      "label": "site-standard-from-engagement-scope"
    }
  ]
}
