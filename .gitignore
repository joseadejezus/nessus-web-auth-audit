# Never commit real engagement data or credentials
credentials.json
*credentials*.json
!credentials.example.json
# Bundled vendor factory defaults are public documentation and part of the
# package — the wildcard above would otherwise silently drop them from the repo.
!src/nwaa/data/default_credentials.json
*.nessus
!tests/fixtures/*.nessus
nwaa-output/
*.png

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/
